# MongoDB development utilities

MongoDB runs locally through the root `docker-compose.yml`. This folder contains versioned index definitions and future synthetic-only seed utilities.

Run indexes after MongoDB is available:

```powershell
python database/scripts/create_indexes.py
```

No student, university, or scholarship records are seeded in the foundation phase.
